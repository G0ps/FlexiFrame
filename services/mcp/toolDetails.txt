{
  "baseUrl": "https://jsonplaceholder.typicode.com",
  "resources": [
    {
      "name": "posts",
      "endPoint": "/posts",
      "supportedMethods": ["GET", "POST"],
      "collectionRoutes": {
        "GET all": {
          "url": "/posts",
          "parameters": null,
          "body": null,
          "description": "Returns list of all posts (100 posts)."
        },
        "POST create": {
          "url": "/posts",
          "parameters": null,
          "body": {
            "title": "string",
            "body": "string",
            "userId": "number"
          },
          "description": "Creates a new post. Returns created post (id will be 101+). Note: data is not actually persisted."
        }
      },
      "singleResourceRoutes": {
        "GET by ID": {
          "url": "/posts/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Get a single post by its ID."
        },
        "PUT update": {
          "url": "/posts/{id}",
          "parameters": ["id"],
          "body": {
            "id": "number",
            "title": "string",
            "body": "string",
            "userId": "number"
          },
          "description": "Replaces the post with new values. Returns updated post (not actually persisted)."
        },
        "PATCH partial update": {
          "url": "/posts/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Updates only provided fields. Returns updated post."
        },
        "DELETE": {
          "url": "/posts/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Deletes a post (fake). Returns empty response or success status (resource not actually removed)."
        }
      },
      "relations": {
        "comments": {
          "url": "/posts/{id}/comments",
          "description": "Get all comments for a post."
        }
      }
    },
    {
      "name": "comments",
      "endPoint": "/comments",
      "supportedMethods": ["GET", "POST"],
      "collectionRoutes": {
        "GET all": {
          "url": "/comments",
          "parameters": null,
          "body": null,
          "description": "Returns list of all comments (500 comments)."
        },
        "GET filtered": {
          "url": "/comments",
          "parameters": {
            "postId": "number"
          },
          "body": null,
          "description": "Returns comments for given postId, e.g. /comments?postId=1."
        },
        "POST create": {
          "url": "/comments",
          "parameters": null,
          "body": {
            "postId": "number",
            "name": "string",
            "email": "string",
            "body": "string"
          },
          "description": "Creates a new comment. Returns created comment (fake)."
        }
      },
      "singleResourceRoutes": {
        "GET by ID": {
          "url": "/comments/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Get a single comment by ID."
        },
        "PUT": {
          "url": "/comments/{id}",
          "parameters": ["id"],
          "body": {
            "id": "number",
            "postId": "number",
            "name": "string",
            "email": "string",
            "body": "string"
          },
          "description": "Replace comment."
        },
        "PATCH": {
          "url": "/comments/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Partial update of comment."
        },
        "DELETE": {
          "url": "/comments/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Delete comment (fake)."
        }
      }
    },
    {
      "name": "albums",
      "endPoint": "/albums",
      "supportedMethods": ["GET", "POST"],
      "collectionRoutes": {
        "GET all": {
          "url": "/albums",
          "parameters": null,
          "body": null,
          "description": "Get all albums (100 albums)."
        },
        "GET filtered": {
          "url": "/albums",
          "parameters": {
            "userId": "number"
          },
          "body": null,
          "description": "Get albums for a given user."
        },
        "POST create": {
          "url": "/albums",
          "parameters": null,
          "body": {
            "userId": "number",
            "title": "string"
          },
          "description": "Create a new album."
        }
      },
      "singleResourceRoutes": {
        "GET by ID": {
          "url": "/albums/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Get album by ID."
        },
        "PUT": {
          "url": "/albums/{id}",
          "parameters": ["id"],
          "body": {
            "id": "number",
            "userId": "number",
            "title": "string"
          },
          "description": "Replace album."
        },
        "PATCH": {
          "url": "/albums/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Partial update."
        },
        "DELETE": {
          "url": "/albums/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Delete album (fake)."
        }
      },
      "relations": {
        "photos": {
          "url": "/albums/{id}/photos",
          "description": "Get all photos in this album."
        }
      }
    },
    {
      "name": "photos",
      "endPoint": "/photos",
      "supportedMethods": ["GET", "POST"],
      "collectionRoutes": {
        "GET all": {
          "url": "/photos",
          "parameters": null,
          "body": null,
          "description": "Get all photos (5000 photos)."
        },
        "GET filtered": {
          "url": "/photos",
          "parameters": {
            "albumId": "number"
          },
          "body": null,
          "description": "Get photos belonging to a given album."
        },
        "POST create": {
          "url": "/photos",
          "parameters": null,
          "body": {
            "albumId": "number",
            "title": "string",
            "url": "string",
            "thumbnailUrl": "string"
          },
          "description": "Create a new photo (fake)."
        }
      },
      "singleResourceRoutes": {
        "GET by ID": {
          "url": "/photos/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Get a photo by ID."
        },
        "PUT": {
          "url": "/photos/{id}",
          "parameters": ["id"],
          "body": {
            "id": "number",
            "albumId": "number",
            "title": "string",
            "url": "string",
            "thumbnailUrl": "string"
          },
          "description": "Replace photo."
        },
        "PATCH": {
          "url": "/photos/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Partial update."
        },
        "DELETE": {
          "url": "/photos/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Delete photo (fake)."
        }
      }
    },
    {
      "name": "todos",
      "endPoint": "/todos",
      "supportedMethods": ["GET", "POST"],
      "collectionRoutes": {
        "GET all": {
          "url": "/todos",
          "parameters": null,
          "body": null,
          "description": "Get all todos (200 todos)."
        },
        "GET filtered": {
          "url": "/todos",
          "parameters": {
            "userId": "number"
          },
          "body": null,
          "description": "Get todos for a given user."
        },
        "POST create": {
          "url": "/todos",
          "parameters": null,
          "body": {
            "userId": "number",
            "title": "string",
            "completed": "boolean"
          },
          "description": "Create a new todo (fake)."
        }
      },
      "singleResourceRoutes": {
        "GET by ID": {
          "url": "/todos/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Get a single todo by its ID."
        },
        "PUT": {
          "url": "/todos/{id}",
          "parameters": ["id"],
          "body": {
            "id": "number",
            "userId": "number",
            "title": "string",
            "completed": "boolean"
          },
          "description": "Replace a todo."
        },
        "PATCH": {
          "url": "/todos/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Partial update of todo."
        },
        "DELETE": {
          "url": "/todos/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Delete a todo (fake)."
        }
      }
    },
    {
      "name": "users",
      "endPoint": "/users",
      "supportedMethods": ["GET", "POST"],
      "collectionRoutes": {
        "GET all": {
          "url": "/users",
          "parameters": null,
          "body": null,
          "description": "Get all users (10 users)."
        },
        "POST create": {
          "url": "/users",
          "parameters": null,
          "body": {
            "name": "string",
            "username": "string",
            "email": "string",
            "address": {
              "street": "string",
              "suite": "string",
              "city": "string",
              "zipcode": "string",
              "geo": {
                "lat": "string",
                "lng": "string"
              }
            },
            "phone": "string",
            "website": "string",
            "company": {
              "name": "string",
              "catchPhrase": "string",
              "bs": "string"
            }
          },
          "description": "Create a new user (fake)."
        }
      },
      "singleResourceRoutes": {
        "GET by ID": {
          "url": "/users/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Get user by ID."
        },
        "PUT": {
          "url": "/users/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Replace user."
        },
        "PATCH": {
          "url": "/users/{id}",
          "parameters": ["id"],
          "body": {},
          "description": "Partial update of user."
        },
        "DELETE": {
          "url": "/users/{id}",
          "parameters": ["id"],
          "body": null,
          "description": "Delete user (fake)."
        }
      },
      "relations": {
        "posts": {
          "url": "/users/{id}/posts",
          "description": "Get all posts by a user."
        },
        "albums": {
          "url": "/users/{id}/albums",
          "description": "Get all albums by a user."
        },
        "todos": {
          "url": "/users/{id}/todos",
          "description": "Get all todos for a user."
        }
      }
    }
  ],
  "notes": {
    "dataPersistence": "All operations (POST, PUT, PATCH, DELETE) are faked: JSONPlaceholder responds as if the action happened, but data is not actually persisted.",
    "responseFormat": "All responses are JSON. All endpoints support HTTP and HTTPS. CORS is enabled for all origins and methods.",
    "filtering": "You can filter collections with query params, e.g. /comments?postId=1, /albums?userId=2 etc.",
    "nestedRoutes": "Nested routes are supported for related resources, e.g. /posts/1/comments, /albums/1/photos, /users/1/posts, /users/1/albums, /users/1/todos."
  }
}
